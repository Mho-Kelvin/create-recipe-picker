# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false


## Environment Properties

minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.13
forge_version_range=[47,)
loader_version_range=[47,)

# Parchment layers community parameter names + javadocs on top of Mojang official mappings.
mapping_channel=parchment
mapping_version=2023.09.03-1.20.1


## Mod Properties

mod_id=createrecipepicker
mod_name=Create Recipe Picker
mod_license=MIT
mod_version=1.0.0
mod_group_id=io.github.mhokelvin.createrecipepicker
mod_authors=Mho Kelvin
mod_description=Resolve Create recipe conflicts (crushing, milling, haunting, splashing) via an in-game GUI. Choices persist per world and apply to every machine of that type.


## Dependency Properties

# Create 6.0.8 for MC 1.20.1, pulled from CurseMaven (project 328085, file 7178761).
# If Create publishes 6.0.8 to a standard maven later, swap to those coords.
create_curse_file_id=7178761
